name: Docker test

on:
  push:
    branches: [ master ]
jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - name: Clone repo
        uses: actions/checkout@v4

      - name: Create docker image
        run: docker build -t gh-actions-test . 
      

